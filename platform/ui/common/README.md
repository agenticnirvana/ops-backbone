# Shared UI chrome

Console shell (`app.js`, `styles.css`, `index.html`, notebooks) stays at `platform/ui/`.
This folder holds **assets every design uses**.

- `logos/mlflow.svg` — MLflow is in D1, D2, and D3

Per-design brands live in `../designs/{d1,d2,d3}/`.

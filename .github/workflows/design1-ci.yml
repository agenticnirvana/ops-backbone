name: Design 1 CI

# Kept so older "Design 1 CI" check names still resolve.
# Enterprise split: ci.yml (lint / unit / secret-scan) + eval-gate.yml + promote.yml

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use workflows ci.yml, eval-gate.yml, and promote.yml."
          echo "This workflow is a compatibility stub (workflow_dispatch only)."

{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "decimals": 2 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
      "targets": [
        { "expr": "service_error_rate{service=\"checkout-service\"}", "legendFormat": "checkout error rate", "refId": "A" }
      ],
      "title": "Checkout error rate (Prometheus)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percent", "decimals": 1 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "service_cpu_percent{service=\"checkout-service\"}", "legendFormat": "CPU", "refId": "A" },
        { "expr": "service_p95_latency_ms{service=\"checkout-service\"}", "legendFormat": "P95 latency ms", "refId": "B" }
      ],
      "title": "Checkout CPU & P95 (Prometheus)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "loki", "uid": "loki" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "options": { "dedupStrategy": "none", "enableLogDetails": true, "showLabels": true, "sortOrder": "Descending", "wrapLogMessage": true },
      "targets": [
        { "expr": "{job=\"agentops\"} |= \"checkout-service\"", "refId": "A" }
      ],
      "title": "Checkout logs (Loki) — set time range Last 30 days",
      "type": "logs"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["agentops", "checkout", "d1"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "D1 · Checkout Redis Pool",
  "uid": "design1-checkout-incident",
  "version": 1
}
